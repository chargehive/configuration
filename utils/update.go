package utils

// Update will load a json file in, merge fields where possible, returns true if changes were made, the json and any errors
func Update(json []byte) (bool, []byte, error) {

	return false, nil, nil
}
